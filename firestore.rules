rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Funkcia na kontrolu, či je užívateľ prihlásený
    function isAuthenticated() {
      return request.auth != null;
    }

    // Funkcia na kontrolu, či je užívateľ admin
    function isAdmin(userData) {
      return isAuthenticated() && userData.role == 'admin';
    }

    // Pomocná funkcia na kontrolu, či je používateľ v rovnakej firme
    function isInSameCompany(companyId) {
      let user = get(/databases/$(database)/documents/users/$(request.auth.uid));
      return request.auth != null && user != null && user.data.companyID == companyId;
    }

    // Pomocná funkcia na kontrolu platnej pozvánky
    function hasValidInvitation(email) {
      let invitations = get(/databases/$(database)/documents/invitations/{invitationId});
      return invitations != null && 
             invitations.data.email == email && 
             invitations.data.status == 'pending';
    }

    // Pravidlá pre kolekciu users
    match /users/{userId} {
      allow read, write: if true; // Povolíme všetky operácie pre všetkých
    }

    // Pravidlá pre kolekciu companies
    match /companies/{companyId} {
      allow read: if request.auth != null;  // Každý prihlásený užívateľ môže čítať údaje o firmách
      allow write: if isAdmin();
    }

    // Pravidlá pre kolekciu invitations
    match /invitations/{invitationId} {
      allow read, write: if true; // Povolíme všetky operácie pre všetkých
    }

    // Pravidlá pre kolekciu vehicles
    match /vehicles/{vehicleId} {
      allow read: if request.auth != null && isInSameCompany(resource.data.companyID);
      allow write: if isAdmin();
    }

    // Pravidlá pre kolekciu routes
    match /routes/{routeId} {
      allow read: if request.auth != null && isInSameCompany(resource.data.companyID);
      allow write: if isAdmin();
    }

    // Pravidlá pre kolekciu settings
    match /settings/{settingId} {
      allow read: if request.auth != null && isInSameCompany(resource.data.companyID);
      allow write: if isAdmin();
    }

    // Pravidlá pre ostatné kolekcie
    match /{document=**} {
      allow read, write: if true; // Povolíme všetky operácie pre všetkých
    }
  }
} 